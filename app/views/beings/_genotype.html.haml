.being-popup.left
  %h5 Genotype 
  %div{ id:"genotype#{being.id}", :class => 'genotype' } 
    - being.genotype.collect{|c| c.to_s }.sort.reverse.each do |chromosome|
      .clear
      - chromosome.split.each do |gene|
        %div{ title:gene, style: "background-color:##{gene};" }  

