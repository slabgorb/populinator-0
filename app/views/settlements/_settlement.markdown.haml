:plain
  ***

  # #{settlement.name}

- settlement.families.each do |family|
  :plain

    ***

    ## Family: #{family.first}

  - family.second.each do |member|
    = render 'beings/being', being: member
